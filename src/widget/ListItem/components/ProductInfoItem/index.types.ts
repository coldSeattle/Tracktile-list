import {TextStyle, ViewStyle} from 'react-native';

type ProductInfoItemT = {
  cStyles?: ViewStyle;
  tStyles?: TextStyle;
  text: string;
  textProps: TextStyle;
};

export type {ProductInfoItemT};
